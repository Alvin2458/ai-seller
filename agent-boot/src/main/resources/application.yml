server:
  port: 8000

spring:
  application:
    name: voice-agent-dashcope-sdk-example
  ai:
    dashscope:
      api-key: sk-41337288817c49a296738f26ea4eb411

# FreeSWITCH Configuration
# FreeSWITCH is running locally (not in Docker)
freeswitch:
  esl:
    host: localhost      # 本机运行的 FreeSWITCH
    port: 8021          # ESL port
    password: ClueCon   # Default FreeSWITCH ESL password
  gateway: default
  caller-id: "1000"
